# The browser always calls same-origin /api. Next.js rewrites that path.
# Optional override of the rewrite target. Defaults to the local backend,
# and to https://radar-strategique.onrender.com when VERCEL=1.
API_PROXY_TARGET=http://127.0.0.1:5000
